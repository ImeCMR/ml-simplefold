# @package _hydra_
run:
  dir: ${paths.output_dir}/runs/${now:%Y-%m-%d}/${now:%H-%M-%S}
sweep:
  dir: ${paths.output_dir}/sweeps/${now:%Y-%m-%d}/${now:%H-%M-%S}
  subdir: ${hydra.job.num}
job_logging:
  handlers:
    file:
      filename: ${hydra.run.dir}/${hydra.job.name}.log
